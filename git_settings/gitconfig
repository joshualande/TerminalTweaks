[user]
    # Username and password for me
	name = Joshua Lande
	email = joshualande@gmail.com
[color]
    # Use color when writing to the terminal, but not to other 
    # output devices
    ui = auto
[color "diff"]
    # This makes changed white space in the 'git diff' red
    whitespace = red reverse
[core]
    # use vim as my editor :)
    editor = /usr/bin/vim
[filter "media"]
	required = true
	clean = git media clean %f
	smudge = git media smudge %f
